<tr>
	<td>
		<%= link_to minus_down_line_items_path(:product_id => line_item.product_id), method: :post do %>
				<%= button_to '-' %>
        <% end %>
	</td>
		<td>				
		<%= link_to line_items_path(:product_id => line_item.product_id),  remote: true, method: :post do %>
				<%= button_to '+' %>
        <% end %>
	</td>
	<td>
		<%= line_item.quantity %> &times;
	</td>

	<td>
		<%= line_item.product.name %>
	</td>
    <td class="item_price">
        <span><%= number_to_currency(line_item.total_price, :unit=>"￥") %></span>
		</td>
		<td>
		<%= link_to line_items_path(:product_id => line_item.product_id), remote: true, method: :delete do %>
			<%= button_to 'Remove' %>
        <% end %>
    </td>
</tr>